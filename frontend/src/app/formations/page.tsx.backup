'use client'

import { useState } from 'react'
import Link from 'next/link'
import Image from 'next/image'

// Données temporaires (en attendant Sanity)
const formationsData = [
  {
    id: 1,
    title: 'CAP Horticulteur',
    slug: 'cap-horticulteur',
    description: 'Maîtrisez les techniques de production végétale, gestion des espaces verts et pépinière.',
    duration: '3 ans',
    price: '350 000 FCFA/an',
    domain: 'agriculture',
    image: '/images/horticulteur.jpg',
  },
  {
    id: 2,
    title: 'CAP Coiffeur',
    slug: 'cap-coiffeur',
    description: 'Coupe, coloration, soins capillaires et gestion de salon.',
    duration: '3 ans',
    price: '400 000 FCFA/an',
    domain: 'beaute',
    image: '/images/coiffeur.jpg',
  },
  {
    id: 3,
    title: 'CAP Couturier Modéliste',
    slug: 'cap-couturier',
    description: 'Création de patrons, confection sur mesure, retouches.',
    duration: '3 ans',
    price: '380 000 FCFA/an',
    domain: 'mode',
    image: '/images/couturier.jpg',
  },
  {
    id: 4,
    title: 'CAP Cuisinier',
    slug: 'cap-cuisinier',
    description: 'Cuisine traditionnelle et internationale, hygiène alimentaire, gestion de stock.',
    duration: '3 ans',
    price: '420 000 FCFA/an',
    domain: 'restauration',
    image: '/images/cuisinier.jpg',
  },
  {
    id: 5,
    title: 'CAP Développement Local',
    slug: 'cap-dev-local',
    description: 'Projets communautaires, gestion associative, collecte de fonds.',
    duration: '3 ans',
    price: '300 000 FCFA/an',
    domain: 'social',
    image: '/images/devlocal.jpg',
  },
  {
    id: 6,
    title: 'CAP Santé Hygiène',
    slug: 'cap-sante',
    description: 'Premiers secours, hygiène en milieu professionnel, prévention.',
    duration: '3 ans',
    price: '360 000 FCFA/an',
    domain: 'sante',
    image: '/images/sante.jpg',
  },
  {
    id: 7,
    title: 'Attestation Coiffure',
    slug: 'att-coiffure',
    description: 'Formation accélérée en coiffure (techniques de base).',
    duration: '3 ans',
    price: '250 000 FCFA/an',
    domain: 'beaute',
    niveau: 'atteste',
  },
  {
    id: 8,
    title: 'Attestation Restauration',
    slug: 'att-restauration',
    description: 'Service en salle, plats simples, hygiène.',
    duration: '3 ans',
    price: '280 000 FCFA/an',
    domain: 'restauration',
    niveau: 'atteste',
  },
  {
    id: 9,
    title: 'Attestation Habillement',
    slug: 'att-habillement',
    description: 'Couture de base, retouches, confection de vêtements simples.',
    duration: '3 ans',
    price: '220 000 FCFA/an',
    domain: 'mode',
    niveau: 'atteste',
  },
]

const domains = ['Tous', 'agriculture', 'beaute', 'mode', 'restauration', 'social', 'sante']

export default function FormationsPage() {
  const [selectedDomain, setSelectedDomain] = useState('Tous')
  const [search, setSearch] = useState('')

  const filteredFormations = formationsData.filter(formation => {
    const matchDomain = selectedDomain === 'Tous' || formation.domain === selectedDomain
    const matchSearch = formation.title.toLowerCase().includes(search.toLowerCase()) ||
                        formation.description.toLowerCase().includes(search.toLowerCase())
    return matchDomain && matchSearch
  })

  return (
    <div className="container mx-auto px-4 py-12">
      <h1 className="text-4xl font-bold text-center mb-4">Nos Formations</h1>
      <p className="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
        Découvrez nos filières professionnelles accessibles du CAP à l’Attestation.
        Formez-vous avec des professionnels et intégrez le marché du travail.
      </p>

      {/* Filtres */}
      <div className="flex flex-col md:flex-row justify-between gap-4 mb-8">
        <div className="flex flex-wrap gap-2">
          {domains.map(domain => (
            <button
              key={domain}
              onClick={() => setSelectedDomain(domain)}
              className={`px-4 py-2 rounded-full text-sm font-medium transition ${
                selectedDomain === domain
                  ? 'bg-primary text-white'
                  : 'bg-gray-200 text-gray-700 hover:bg-gray-300'
              }`}
            >
              {domain === 'Tous' ? 'Tous' : domain.charAt(0).toUpperCase() + domain.slice(1)}
            </button>
          ))}
        </div>
        <input
          type="text"
          placeholder="Rechercher une formation..."
          value={search}
          onChange={(e) => setSearch(e.target.value)}
          className="border rounded-lg px-4 py-2 w-full md:w-64"
        />
      </div>

      {/* Grille des formations */}
      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {filteredFormations.map(formation => (
          <div key={formation.id} className="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-lg transition">
            <div className="h-48 bg-gray-300 relative">
              {/* Image placeholder (remplacer par next/image si images disponibles) */}
              <div className="w-full h-full bg-gradient-to-r from-primary/20 to-secondary/20 flex items-center justify-center text-gray-500">
                📸 Image de {formation.title}
              </div>
            </div>
            <div className="p-6">
              <div className="flex justify-between items-start mb-2">
                <h2 className="text-xl font-bold">{formation.title}</h2>
                <span className="text-xs bg-primary/10 text-primary px-2 py-1 rounded-full">
                  {formation.niveau === 'atteste' ? 'Attestation' : 'CAP'}
                </span>
              </div>
              <p className="text-gray-600 mb-4">{formation.description}</p>
              <div className="flex justify-between items-center text-sm text-gray-500 mb-4">
                <span>⏱️ {formation.duration}</span>
                <span>💰 {formation.price}</span>
              </div>
              <Link
                href={`/formations/${formation.slug}`}
                className="inline-block bg-primary text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition text-center w-full"
              >
                En savoir plus
              </Link>
            </div>
          </div>
        ))}
      </div>
      {filteredFormations.length === 0 && (
        <p className="text-center text-gray-500 mt-12">Aucune formation trouvée.</p>
      )}
    </div>
  )
}
